steps:

  - label: "Build Dev :docker: image"
    command: "auto/build"
    env:
      ENVIRONMENT: "DEV"
    agents:
      queue: 'eks-dataservices-agent:ci'
      aws:account-id: 'awscloud.data-engineering.development'
    branches: "buildkite-integration"

  - block: "Deploy to :kubernetes: Cluster!!!"

  - label: "Deploy to :kubernetes: Dev "
    command: "auto/deploy"
    env:
      ENVIRONMENT: "DEV"
    agents:
      queue: 'eks-dataservices-agent:ci'
      aws:account-id: 'awscloud.data-engineering.development'
    branches: "buildkite-integration"